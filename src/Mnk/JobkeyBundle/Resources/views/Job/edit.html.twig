{% extends '::base.html.twig' %}

{% block body -%}
	<div class="center-block">
        <h1>Job edit</h1>
        <br>
    </div>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mnk_job') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
